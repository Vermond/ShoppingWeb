# 프로젝트 작업 지침

이 저장소는 하나의 서비스에 대한 프론트엔드와 백엔드를 함께 관리한다.

기본 구조:

```text
/
├─ frontend/   # Next.js + React + TypeScript
├─ backend/    # NestJS + TypeScript
└─ AGENTS.md
```

하위 디렉터리에 별도의 `AGENTS.md`가 존재하면 해당 디렉터리에서는 하위 지침을 함께 따른다.

특히 `frontend/AGENTS.md`에 Next.js가 자동 생성한 지침이 존재할 수 있으므로 임의로 삭제하거나 루트 파일로 이동하지 않는다.

---

## 1. 작업 전 확인

코드를 수정하기 전에 반드시 관련 파일과 현재 구조를 먼저 확인한다.

기존 구현을 충분히 확인하지 않은 상태에서 새로운 구조, 추상화, 패턴을 도입하지 않는다.

기존 코드로 해결 가능한 문제에 새로운 라이브러리나 프레임워크를 추가하지 않는다.

새로운 외부 패키지가 필요한 경우:

1. 왜 필요한지 확인한다.
2. 현재 의존성으로 해결할 수 없는지 확인한다.
3. 유지보수되고 있는 패키지인지 확인한다.
4. 설치가 필요한 이유를 작업 결과에 명시한다.

명시적인 요청 없이 패키지를 추가하지 않는다.

---

## 2. 변경 범위

요청받은 범위 안에서 최소한의 변경을 우선한다.

관련 없는 코드까지 함께 리팩터링하지 않는다.

대규모 구조 변경과 기능 변경을 한 번에 수행하지 않는다.

가능하면 다음과 같이 작업을 분리한다.

1. 현재 구조 분석
2. 구조 개선
3. 기능 변경
4. 스타일 변경
5. 검증

기존에 정상 동작하는 기능과 디자인을 명확한 이유 없이 변경하지 않는다.

---

## 3. 코드 가독성

사람이 읽고 수정하기 쉬운 코드를 우선한다.

과도하게 짧거나 압축된 코드를 작성하지 않는다.

여러 의미를 가진 처리를 한 줄에 몰아넣지 않는다.

조건문, JSX, CSS 등은 가독성을 위해 적절히 줄바꿈한다.

소스 코드를 수동으로 minify하지 않는다.

Minification과 production optimization은 빌드 도구에 맡긴다.

---

## 4. 컴포넌트 분리

컴포넌트는 역할과 책임을 기준으로 분리한다.

파일 크기나 줄 수만을 이유로 기계적으로 분리하지 않는다.

다음과 같은 경우 분리를 고려한다.

- 독립적인 UI 영역을 구성하는 경우
- 자체 상태나 이벤트 처리를 가지는 경우
- 여러 위치에서 재사용되는 경우
- 부모 컴포넌트의 책임을 명확하게 줄일 수 있는 경우
- 테스트 단위로 분리하는 것이 자연스러운 경우

다음과 같은 과도한 분리는 피한다.

- 단순한 `div` 하나를 위한 컴포넌트
- 한두 줄의 JSX만 감싸는 의미 없는 컴포넌트
- 실제 재사용 가능성이 없는 과도한 공통화
- 단순히 파일을 짧게 만들기 위한 분리

컴포넌트 이름만 보고 역할을 이해할 수 있도록 작성한다.

---

## 5. 프론트엔드 기본 원칙

프론트엔드는 다음 기술을 기준으로 한다.

- Next.js
- React
- TypeScript
- App Router
- MUI

현재 설치된 Next.js 버전의 동작과 API를 우선한다.

Next.js 관련 코드를 작성하거나 수정하기 전에 `frontend/AGENTS.md`에 자동 생성된 Next.js 지침이 있다면 반드시 따른다.

필요한 경우 설치된 Next.js 패키지 내부의 현재 버전 문서를 확인한다.

과거 Next.js 버전의 관습을 현재 프로젝트에 무조건 적용하지 않는다.

---

## 6. MUI 사용 원칙

일반적인 UI 컴포넌트는 MUI 사용을 우선 검토한다.

예:

- Button
- IconButton
- TextField
- Select
- Menu
- Dialog
- Drawer
- Snackbar
- Chip
- Pagination
- Skeleton
- Tooltip

반복되는 색상, 간격, typography 등은 가능한 경우 MUI Theme으로 관리한다.

단, 모든 스타일을 억지로 MUI로 변환하지 않는다.

다음과 같은 경우 CSS Module을 사용해도 된다.

- 복잡한 장식 표현
- CSS pseudo element 활용
- 특수 애니메이션
- 복잡한 반응형 시각 효과
- CSS만으로 그린 그래픽 요소
- MUI로 표현했을 때 오히려 코드가 복잡해지는 경우

MUI와 CSS Module 중 어느 쪽이 더 단순하고 유지보수하기 쉬운지를 기준으로 선택한다.

---

## 7. CSS 작성 규칙

CSS 소스는 사람이 읽을 수 있는 형태로 작성한다.

다음과 같이 속성을 한 줄에 몰아넣지 않는다.

```css
.example { display:flex;align-items:center;padding:16px;margin:0; }
```

다음과 같이 작성한다.

```css
.example {
  display: flex;
  align-items: center;
  padding: 16px;
  margin: 0;
}
```

반응형 스타일은 의미 있는 breakpoint 단위로 정리한다.

동일하거나 매우 유사한 스타일이 반복되면 공통화 가능성을 검토한다.

단, 공통화를 위해 지나치게 복잡한 selector나 추상화를 만들지 않는다.

`globals.css`에는 실제 전역 스타일만 둔다.

특정 페이지나 컴포넌트에만 적용되는 스타일은 가능한 경우 해당 범위의 CSS Module에 둔다.

---

## 8. TypeScript

가능하면 명시적인 타입을 유지하고 `any` 사용을 피한다.

타입 안정성을 우회하기 위해 불필요한 type assertion을 사용하지 않는다.

반복되는 도메인 타입은 적절한 위치에 정의하고 재사용한다.

단순한 로컬 타입까지 무조건 별도 파일로 분리하지 않는다.

런타임 데이터는 TypeScript 타입만 믿지 말고 필요한 경계에서 실제 유효성 검사를 고려한다.

---

## 9. 상태 관리

React 상태는 필요한 최소 범위에 둔다.

하위 컴포넌트에서만 필요한 상태를 무조건 최상위 컴포넌트로 올리지 않는다.

반대로 여러 컴포넌트가 공유하는 상태를 중복해서 관리하지 않는다.

현재 규모에서 React 기본 상태 관리로 충분한 경우 별도 상태 관리 라이브러리를 추가하지 않는다.

상태 관리 라이브러리는 실제 복잡성이 발생했을 때 검토한다.

---

## 10. 백엔드 기본 원칙

백엔드는 다음 기술을 기준으로 한다.

- Node.js
- NestJS
- TypeScript
- PostgreSQL
- Prisma

NestJS의 module / controller / service 책임을 구분한다.

Controller에 비즈니스 로직을 집중시키지 않는다.

DB 접근 로직이 여러 위치에 무질서하게 퍼지지 않도록 한다.

Prisma schema 변경 시 기존 데이터와 migration 영향을 함께 확인한다.

API 입력값을 신뢰하지 않고 적절한 validation을 적용한다.

---

## 11. 프론트엔드와 백엔드 경계

프론트엔드에서 직접 데이터베이스에 접근하지 않는다.

DB 접근은 백엔드를 통한다.

민감한 키와 서버 전용 인증 정보는 프론트엔드 코드에 포함하지 않는다.

API contract를 변경할 경우 프론트엔드와 백엔드 양쪽 영향을 확인한다.

UI를 위해 백엔드 내부 구현 상세를 프론트엔드에 노출하지 않는다.

---

## 12. 인증 및 보안

인증은 Access Token + Refresh Token 구조를 기준으로 한다.

토큰, 비밀번호, Secret Key 등을 저장소에 커밋하지 않는다.

다음 파일 및 값은 특히 주의한다.

- `.env`
- DB password
- JWT secret
- Supabase service role key
- OAuth client secret
- Toss Payments secret key

클라이언트에서 반드시 공개되어야 하는 값과 서버에서만 사용해야 하는 값을 구분한다.

보안을 약화시키는 임시 구현을 실제 구현처럼 남겨두지 않는다.

---

## 13. 환경 변수

환경별 설정은 소스 코드에 직접 하드코딩하지 않는다.

환경 변수 이름은 역할을 명확히 알 수 있게 작성한다.

실제 secret이 포함된 `.env` 파일은 Git에 포함하지 않는다.

필요하면 `.env.example`을 제공하되 실제 비밀 값은 넣지 않는다.

---

## 14. 테스트와 검증

작업 완료 후 수정 범위에 맞는 검증을 수행한다.

프론트엔드에서는 가능한 경우 다음을 확인한다.

```bash
npm run lint
npm run build
```

테스트가 존재하면 관련 테스트도 실행한다.

백엔드에서도 lint, build, test가 설정되어 있다면 변경 범위에 맞게 실행한다.

검증 실패를 숨기지 않는다.

작업 전부터 존재한 오류인지 이번 변경으로 발생한 오류인지 구분해서 보고한다.

---

## 15. 기존 디자인 보존

디자인 변경 요청이 없는 경우 현재 디자인을 최대한 유지한다.

구조 리팩터링 과정에서 다음을 임의로 바꾸지 않는다.

- 색상
- 글꼴
- 간격
- 레이아웃
- 반응형 동작
- 애니메이션
- 사용자에게 보이는 문구

구조 개선과 디자인 변경을 가능하면 별도 작업으로 구분한다.

---

## 16. 작업 결과 보고

작업 후 다음 내용을 간결하게 정리한다.

- 무엇을 변경했는지
- 왜 변경했는지
- 중요한 구조적 판단
- 새로 추가한 dependency가 있는지
- 실행한 검증
- 남아 있는 문제나 주의점

수정하지 않은 사항을 수정했다고 보고하지 않는다.

검증하지 않은 사항을 정상이라고 단정하지 않는다.